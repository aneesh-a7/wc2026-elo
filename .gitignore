__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Large raw source data — regenerated by `python pipeline.py` (fetch step).
# Committing keeps the repo runnable offline; uncomment to exclude if preferred.
# data/intl_results.csv
# data/shootouts.csv
